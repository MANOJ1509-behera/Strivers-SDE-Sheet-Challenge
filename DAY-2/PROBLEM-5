public class Solution {
	public static void deleteNode(LinkedListNode<Integer> node) {
        LinkedListNode<Integer> node2 = node.next;
        node.data = node2.data;
        node.next = node2.next;

		
	}
}
